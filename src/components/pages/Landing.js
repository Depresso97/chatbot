import React from 'react';

const Landing = () => (
    <div style={{ textAlign: 'center' }}>
        <h1>SNET SAP chatbot</h1>
        Demo page for the chatbot
        </div>
    )

export default Landing;